from setuptools import setup, find_packages

setup(
    # Project name
    name="llm_reconstruction_free",
    # Version number of your project
    version="0.1.0",
    # Short description of your project
    description="Code for LLM project",
    # List runtime dependencies here. These will be installed by pip when your
    # project is installed.
    install_requires=["numpy"],
)
